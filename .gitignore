# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# venv
.venv/
venv/

# editor
.vscode/
.idea/

# local bus data — never commit
*.db
*.db-wal
*.db-shm
audit.log
.claude-agent-bus/

# Claude Code user-local state (settings, scheduled tasks, worktrees)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/

# Per-machine agent-bus wiring written by `agent-bus init`.
# Contains absolute paths to the local pipx venv; regenerate per-machine.
.mcp.json
.claude/settings.json
